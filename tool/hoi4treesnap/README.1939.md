"""Vendored cpntodd/hoi4treesnap (v0.5.12 Go sources) + names-only layout PNG.

Go GUI binary needs Fyne/network; the 1939 service uses ``layout_png.py`` which
follows the same ``focus_spacing`` / absolute-position rules as treesnap
(``render.go`` / ``misc.go``), drawing plaques + localized names only (no icons).
"""
